# RedisPersistentModule
A redis module which store the data in file . Useful when persistence on disk is needed for big value.

### usage

```
//GET
PERSIST.GET <key>

//SET
PERSIST.SET <key> <value>
````
